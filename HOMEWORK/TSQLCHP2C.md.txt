TSQLCH3

1. What is a data type? Why do we have data types?
-  is an attribute that specifies the type of data that the object can hold

2. What is a collation? Name four elements of a collation
- collation is a property of character data that encapsulates several aspects

3. How would you strip whitespace from a string? 
- by using the TRIM function

4. Suppose you wanted to make a list of every college and university that was call an Institue from the colleger table. Write the query.

5. How you find out the index of the first space in a string?

6. How would you select just the first name in a list of a the presidents. First names can be and arbitrary length, from "Cal" to "Benjamin".'

7. Payments are due exactly 30 days from the date of the last function. Write a select query that calculates the date of the next payment. Pretend we want to update a column in a database that contains the date of the next payment. We will do this when we write UPDATE queries

8. Suppose your son or daughter wants to run a query every day that tells theme the number of days until thier 16th birthday. Write a select query that does this.

9. What function returns the current date? This is very useful in a table that maintains a log of events, such as user login.